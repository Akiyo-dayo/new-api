# 千咲主题定制版更新日志（Chisa Edition Changelog）

本文件记录 Akiyo-dayo/new-api `chisa` 分支相对上游的定制改动。
线上部署镜像 tag 格式：`v1.0.0-rc.22-chisa.N`。

---

## v1.0.0-rc.22-chisa.30（2026-08-28）· 倍率区间令牌与模型广场定价改造

### 新增

- **倍率区间令牌**：令牌分组可填 `ratio:<下界>-<上界>`（与 `auto` 同类的伪分组），语义是
  **最低价优先、同价轮询**。候选分组按该用户实际生效的倍率（`GroupGroupRatio` 覆盖价优先于
  `GroupRatio`）升序分层，先在最便宜那层内轮询；该层没有该模型的可用渠道、或调用失败降级过来，
  才走下一层。区间的含义是「我最多接受到这个价」。候选只来自用户本来就可用的分组，拿不到区间外的分组。
- **模型广场**：分组展示名与归类配置、按分组的价格档、真实速率限制（不再是随机假数据）、
  供应商归属改为确定性的最左匹配、新增 `price_configured` 字段——没配过价的模型不再显示
  兜底倍率算出来的假价，而是显示「价格未配置」并排到列表末尾。
- **计费表达式保存前校验**：`billing_setting.billing_expr` 写入时逐条编译并试算
  （`SmokeTestExpr` 此前是零调用的死代码）。编译不过的表达式会被拒绝并点名是哪个模型，
  不再出现「广场标着价、调用时求值失败退到预扣兜底额」。定时同步不经过校验，
  库里已有的表达式仍照常加载。
- **停机 flush**：进程退出前落一次批量更新缓冲区与性能采样桶。此前每次停机都会丢掉最多一个
  `BATCH_UPDATE_INTERVAL` 的扣费，而消费日志早已写好。

### 修复

- **渠道亲和不再把区间令牌钉在更贵的价位上。** 亲和记的是上次成功的渠道，包括便宜层短暂不可用时
  降级过去的贵渠道；便宜层恢复后没人重新比价，一钉就是一个 TTL（默认 3600 秒）。
  现在回推出计费分组后会再比一次价，比当下最低可用价贵就放弃亲和、退回选路。只对 `ratio:` 生效，
  `auto` 的顺序回退语义不变。
- **定价编辑器不再静默改写库里的表达式。** 三类：时间条件的 `<=` / `>` 只能解析不能构造，
  打开编辑器就被改成 `>=`（深夜半价会变成白天半价）；`param(...)` 的等值字面量类型丢失，
  `== "5"` 被改写成 `== 5`；`header(...)` 的等值比较写成裸数字，在 expr-lang 里是
  `mismatched types string and int`，整条表达式编译不过。
- **多档表达式的非末档缺条件时不再产出裸 `:`**（语法错误），改为 fail-closed。
- **半途编辑不再冲掉库里的表达式**：配置非空但拼不出串时不写回空串。
- **广场能读回编辑器自己生成的 `param` 规则**。此前 `param(X) != nil && <对X的条件>` 这种两项
  `&&` 被拆开后解析不回来，整条规则判空，该模型在广场上一个价格都没有。
- **计价系数不再静默漏读**：`15 * c`（数字在前）、`p*3 + p*5`（同变量出现两次）、
  归因不到变量的剩余项。此前这些会显示一个偏低的价且不触发「无法解析」告警。
- **前端只认 `v1:` 前缀**，与后端 `ParseExprVersion` 对齐。此前会把 `v2:` 剥掉按 v1 语义标价，
  而后端拿到 `v2:` 是整串进编译器并在冒号上语法错误。
- **`RLock` 当写锁用导致的进程崩溃**：`setting/rate_limit.go` 在读锁下重建 map，与每 60 秒一次的
  option 同步撞上会 `fatal error: concurrent map read and map write`。同类问题一并修了
  `setting/auto_group.go`（此前连读锁都没有，读到空列表会让 `auto` 令牌直接 503）和
  `setting/operation_setting/status_code_ranges.go`。三处的写入都改成先解析到局部再整体替换，
  配置损坏时保留上一份而不是留下一张空表。
- **批量落库失败不再无条件重放**。三个落库函数都是不带幂等键的 `col = col + ?`，服务端已提交、
  客户端没拿到应答时重放就是同一笔扣费扣两次。现在按「服务端拒绝」（确定没生效，重放）和
  「连接出事」（不可判定，丢弃并留可对账日志）区分。
- **令牌分组校验**：创建与更新时校验分组是否为该用户可用；只在用户真的改了分组时校验，
  不会让绑着已下线分组的老令牌从此改不动。
- 结算失败时按**实扣额**记账并在日志 `admin_info` 留痕；上游没有返回可计费用量时归零，
  阶梯计费的兜底不再覆盖这个归零。
- 阶梯计费快照的分组倍率跟随跨分组降级刷新。
- 详情页「基础价格」与其下的价格档表格改用同一个货币格式化器，不再对同一个系数给出两个数。
- 区间令牌编辑器：定价目录未加载完时不再把已选模型全判成「区间外」并提示一键移除
  （点下去会清空白名单，令牌从受限变成区间内全模型可调）。

### 运维注意

- **容器停止宽限期**：`http.Server.Shutdown` 会等流式请求结束（`SHUTDOWN_TIMEOUT_SECONDS`
  默认 120 秒），停机 flush 排在它之后。Docker 默认 10 秒后 SIGKILL，只要停机时有一条 SSE
  在跑，flush 就执行不到。部署时请给 new-api 服务设 `stop_grace_period`（建议 130s），
  或把 `SHUTDOWN_TIMEOUT_SECONDS` 调到宽限期以内。
- **`RETRY_TIMES` 与区间令牌**：区间令牌恒定允许跨分组重试，且每换一个分组重试预算会重置，
  单次请求最多产生 `(RETRY_TIMES + 1) × 区间内候选分组数` 次上游调用（限流只计 1 次）。
  调大 `RETRY_TIMES` 前请评估上游成本。
- **模型请求限流只挂在 `/v1` 与 `/v1beta` 两个路由组上**，`/pg`、`/mj`、`/suno` 与
  `/v1/videos`（在独立的 group 上注册）不受分组或全局请求数限制。

---

## v1.0.0-rc.22-chisa.29（2026-08-21）· 生产定价显示与主线转正

### 修复

- 动态计费表达式的外层数值倍率会正确作用于各阶梯价格。
- 表达式拆分保留版本前缀与请求规则，并拒绝无法安全解析的阶梯算术，避免展示错误价格。
- 增加定价表达式与动态价格专项回归测试。

### 主线

- 将已在生产验证的公告、消费排行榜、精确模型日志筛选、上海自然周期排行榜和定价显示修复正式收敛到 `chisa` 主线。
- 后续生产构建仅以 `Akiyo-dayo/new-api:chisa` 的固定提交为源码来源。

---

## v1.0.0-rc.22-chisa.28（2026-08-16）· 排行榜时间范围

### 新增

- 排行榜默认恢复原仓滚动窗口：今天 24 小时、本周 7 天、本月 30 天、今年 365 天。
- 新增个人本地“滚动窗口 / 自然周期”切换；选择保存在浏览器本地，不影响其他用户。
- 自然周期的“今天 / 本周 / 本月 / 今年”统一按 `Asia/Shanghai` 计算，图表时间标签同步使用该时区。

### 兼容性

- `/api/rankings` 的 `mode` 参数缺省或非法时回退到 `rolling`，旧客户端无需修改。
- 本次发布仅更新 `new-api` 应用容器，不涉及 PostgreSQL、Redis、channels、abilities 或 pricing options。

---

## v1.0.0-rc.22-chisa.25（2026-08-08）· 卡网商店

### 新增

- **学分账户（wallet）页内嵌卡网商店**：直接在站内购买兑换码（1 / 10 / 30 学分），无需跳转第三方卡网。支持支付宝 / 微信渠道，数量选择、优惠码、实时价格计算；下单后新窗口拉起支付，站内自动轮询支付状态，支付成功直接展示卡密并支持一键复制（对接 catfk 鲸商城PRO 店铺开放接口，字段与官方前端对齐 `response.cards`）。
- **卡密订单查询**：充值卡片右上角新增入口，凭下单时填写的联系方式 + 图形验证码查询历史订单、订单详情与卡密，支持带查询密码的商品。
- 新增 `/fk-api/*` 同源代理通道（开发环境 rsbuild proxy，生产环境 Caddy 反代）→ `catfk.com/shopApi/*`。

### 变更

- 移除充值卡片右上角「订单历史」入口，仅保留「卡密订单查询」。
- 卡网接口从浏览器跨站直连改为同源代理：验证码图片绑定 PHP 会话 Cookie（SameSite=Lax），浏览器跨站一律不发送导致图片无法显示，代理后 Cookie 转为第一方彻底根治。

### 修复

- 修复代理模式下接口路径双前缀（`/shopApi/shopApi/...`）导致全部卡网请求 404 的问题。
- 修复验证码图片防缓存时间戳在渲染期生成、输入字符即触发图片反复刷新的问题（改为加载验证码时固定一次）。

### 部署说明

- 前端：`web/src/features/wallet/cardshop/`（新）、`recharge-form-card.tsx`、`rsbuild.config.ts`。
- 生产 Caddy 需包含：`handle_path /fk-api/* { rewrite * /shopApi{uri}; reverse_proxy https://catfk.com }`。
- 镜像：`new-api-chisa:v1.0.0-rc.22-chisa.25-cardshop-20260808`（基于 chisa.24-zhipu-v4-responses，后端无改动）。

---

## v1.0.0-rc.22-chisa.16 ~ chisa.24（2026-07-30 ~ 2026-08-03）

> 此区间版本当时以服务器本地构建树迭代，未及时提交；2026-08-15 已核对线上运行源码树并回补到本仓库。

- **chisa.16 ~ 18（2026-07-30）**：定价页分组筛选面板美化——家族聚类列表、倍率着色徽章、内联圆点筛选（消除列表位移）、描述文字换行修复（对应提交 8a081b7 / 6947086 / 3eda540；chisa.18 为同状态重建）。
- **chisa.20（2026-07-30）**：定价页人民币（CNY）汇率显示，保留底价基准与侧边栏布局（`web/src/features/pricing/lib/price.ts`）。
- **chisa.21（2026-07-30）**：价格值美元符号显示调整，保留数值不变。
- **chisa.22（2026-07-30）**：Claude tool index 修复。
- **chisa.23（2026-07-31）**：合并 claude-tool-index 与 lm-dollar 显示调整。
- **chisa.24（2026-08-03）**：智谱 `zhipu_4v` 渠道接入 relaykit Responses 格式转换（`relay/channel/zhipu_4v/adaptor.go`，新增 `adaptor_responses_test.go` / `adaptor_responses_dispatch_test.go`）。

---

## v1.0.0-rc.22-chisa.15（2026-07-30）· 星炬学院教务系统

**主题概念升级：千咲主题 × 星炬学院（Star Torch Academy）教务系统**

### 首页（Landing）
- 徽章改为「星炬学院 · 教务处」（千咲任教务处值勤）
- 渐变主标语改为「万模一证通行」
- 副文案：「没有早八，没有挂科——一张学生证，玩遍全球大模型。」
- 按钮：「入学登记」（注册）/「学费标准」（定价）；登录后显示「进入教务系统」+「学费标准」
- 底部能力行：「学籍秒开 · 按量计学分 · 全科通行」
- 新增左下角旋转校徽印章：环形金字 `STAR TORCH ACADEMY · 星炬学院 · REGISTRAR`，中心为白色「A」院徽（新增静态资源 `web/public/chisa/sta-emblem.png`），36s 缓转，支持 `prefers-reduced-motion`
- 页脚右侧改为「教务值勤 · 朽叶千咲」

### 登录 / 注册页
- 标题区重构：眉题「星炬学院 · 教务处」+ 金色渐隐细分隔线 + 大标题「返校登录」/「入学登记」
- 左下角新增学院落款条：「A」院徽 +「星炬学院」+「学籍秒开 · 按量计学分 · 全科通行」
- 右下角名言卡顶部加「星炬学院 · 学籍管理处」落款
- 修复：左上角 Logo / 站点名点击无法返回首页（表单容器与 Logo 链接同为 z-10 且 DOM 靠后导致点击被拦截，Logo 提升至 z-20）

### 中文学分体系（仅 zh 词条，英文不受影响）
- 钱包 → 学分账户；当前余额 → 当前学分；剩余配额 → 剩余可用学分
- 总消耗额度 → 累计消耗的学分；余额不足 → 学分不足
- 控制台「添加额度」→ 充入学分；「生产流量前保持充足余额」→ 正式接入前确保学分充足

### 运维
- 登录 429 排查结论：限频计数持久化在 Redis（`rateLimit:v2:ip:CT:<IP>`），重启容器无效；删除对应键即可恢复

---

## v1.0.0-rc.22-chisa.14（2026-07-29）· 品牌更名

- 全站品牌名「星矩学院」→「星炬学院」；英文 `Star Matrix Academy` → `Star Torch Academy`
- About 页脚诗句「星光自有其矩」→「星光自有其炬」
- 同步 Komari 监控面板节点名、标签、主题注入文案与站点设置

## v1.0.0-rc.22-chisa.13（2026-07-29）· About 页服务器状态

- About 页重构为 Komari 服务器状态面板（千咲主题），与 status.akiyo.fun 面板样式统一
- 统计带（当前在线 / 点亮地区 / 流量概览 / 网络速率）、服务器卡片标签（价格 / 计费周期 / 带宽）、多节点自适应布局
- 修复长期 / 一次性 / 免费等计费标签识别

## 更早版本

- chisa.12 及以前：千咲主题首页（海报式 Landing）、登录注册页立绘与花瓣氛围、About 页初版、Komari 面板千咲主题注入等，详见 Git 历史。
